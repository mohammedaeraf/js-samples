// Initialize an array with some numbers
const numbers = [1, 2, 3, 4, 5];

// Use the map function to create a new array with each number multiplied by 100
let squared = numbers.map(n => n * 100);

// Output the new array with squared numbers to the console
console.log(squared); // Output: [100, 200, 300, 400, 500]