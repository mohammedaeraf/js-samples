// Initialize an array with some numbers
const numbers = [1, 2, 3, 4, 5];

// Use the reduce function to calculate the sum of all numbers in the array
const sum = numbers.reduce((total, n) => total + n);

// Output the sum to the console
console.log(sum); // Output: 15